# Copiar este archivo como .env y completar con los valores reales.
# .env no se versiona: contiene la contrasena de la base de datos.

PGHOST=localhost
PGPORT=5432
PGDATABASE=amasaapp
PGUSER=amasaapp
PGPASSWORD=

PORT=3000
